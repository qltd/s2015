(function ($) {
  Drupal.behaviors.s2015FrontCountdown = {
    attach: function () {
      var container = $('.front-countdown');
      if (!container.length) return;

      var date = new Date();
    }
  };
})(jQuery);
